1:
  status: observed
  argument: User only communicates with the Zuul gateway server.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/RecommendationClientApplication.java
      lines: [12]

2:
  status: disregarded
  argument: Services communicate internally over plain HTTP (See example artifacts). The communication is not authenticated.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/service/RecommendationClientService.java
      lines: [42, 54, 87]
    - file: recommendation-service/src/main/java/com/example/MyConfiguration.java
      lines: [33]

3:
  status: disregarded
  argument: No authentication mechanism is deployed.

4:
  status: disregarded
  argument: External entities are not represented in the application. Users behave transparent to the server. Note, that the server has a user-service, that is not used for authentication purposes.

5:
  status: disregarded
  argument: No authentication tokens are used.

6:
  status: disregarded
  argument: No rate limiting of any means is deployed.

7:
  status: disregarded
  argument: User external entities can call the gateway service using an unencrypted HTTP connection.

8:
  status: disregarded
  argument: All internal services communicate over insecure HTTP connections.

9:
  status: disregarded
  argument: No central logging system is deployed.

10:
  status: disregarded
  argument: No central logging system is deployed. Two internal services perform local logging, however none of the logs are consumed and accessed through monitoring software.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/MainController.java
      lines: [51]
    - file: recommendation-client/src/main/java/com/example/service/RecommendationClientService.java
      lines: [38, 43, 50, 56, 80, 88]

11:
  status: disregarded
  argument: Logs are not sanitized and could possibly contain sensitive information when exceptions in the recommendation service are logged.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/MainController.java
      lines: [51]

12:
  status: disregarded
  argument: No logs are collected and no message broker is deployed.

13:
  status: observed
  argument: Hystrix is deployed as a circuit breaker on the gateway server.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/RecommendationClientApplication.java
      lines: [15]

14:
  status: observed
  argument: The gateway server uses load balancing via Ribbon to access dependent services.
  artifacts:
    - file: recommendation-client/src/main/java/com/example/RecommendationClientApplication.java
      lines: [12]

15:
  status: not applicable
  argument: Not a service mesh deployment.

16:
  status: observed
  argument:
    - Registry Service (Eureka Server) with @EnableEurekaServer present.
    - Project is missing infrastructure description, not certain if and how the discovery server is deployed.
    - Services are not explicitly registered with Eureka and wont be secured by default.
  artifacts:
    - file: eureka-service/src/main/java/com/example/EurekaServiceApplication.java
      lines: [7]
    - file: eureka-service-default.yml
      repository: mdeket/spring-cloud-example-config-repo

17:
  status: disregarded
  argument: No HTTP basic password listed in any YML-Configuration of format username:password@here-location-of-eureka-server at "eureka.client.serviceUrl.defaultZone".
  artifacts:
    - file: eureka-service/src/main/java/com/example/EurekaServiceApplication.java
      lines: [7]
    - file: movie-service-default.yml
      repository: mdeket/spring-cloud-example-config-repo

18:
  status: disregarded
  argument: No secret manager is deployed. The deployed git configuration server is not a secrets manager.
  artifacts:
    - file: config-service/src/main/resources/application.properties
      lines: [3]
