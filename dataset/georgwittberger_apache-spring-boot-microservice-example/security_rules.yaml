1:
  status: observed
  argument: The application uses Apache httpd as a gateway server.
  artifacts:
    - file: apache-configuration/httpd-microservice-example.conf
      lines: [22]

2:
  status: disregarded
  argument: Services communicate internally over plain HTTP (See example artifacts). The communication is not authenticated.
  artifacts:
    - file: apache-configuration/httpd-microservice-example.conf
      lines: [27, 31, 35]

3:
  status: disregarded
  argument: No authentication mechanism is deployed.

4:
  status: disregarded
  argument: External entities are not represented in the application. Users behave transparent to the server. Note, that the server has a user-service, that is not used for authentication purposes.

5:
  status: disregarded
  argument: No authentication tokens are used.

6:
  status: disregarded
  argument: No rate limiting of any means is deployed.

7:
  status: disregarded
  argument: User external entities can call the gateway service using an unencrypted HTTP connection.
  artifacts:
    - file: apache-configuration/httpd-microservice-example.conf
      lines: [22]

8:
  status: disregarded
  argument: All internal services communicate over insecure HTTP connections.
  artifacts:
    - file: cart-service/src/main/resources/application.yml
      lines: [2]
    - file: content-service/src/main/resources/application.yml
      lines: [2]
    - file: product-service/src/main/resources/application.yml
      lines: [2]

9:
  status: disregarded
  argument: No central logging system is deployed.

10:
  status: disregarded
  argument: No central logging system is deployed.

11:
  status: disregarded
  argument: No logs are generated by the application.

12:
  status: disregarded
  argument: No message broker is deployed and no logging is performed.

13:
  status: disregarded
  argument: No explicit circuit breaker is deployed.

14:
  status: disregarded
  argument: No load balancing is deployed.

15:
  status: not applicable
  argument: Not a service mesh deployment.

16:
  status: disregarded
  argument: No service registry service is deployed. Services are statically linked.
  artifacts:
    - file: apache-configuration/httpd-microservice-example.conf
      lines: [27, 31, 35]

17:
  status: disregarded
  argument: No service registry service is deployed. Services are statically linked.
  artifacts:
    - file: apache-configuration/httpd-microservice-example.conf
      lines: [27, 31, 35]

18:
  status: disregarded
  argument: No secret manager is deployed.
