1:
  status: observed
  argument: User only communicates with the Zuul gateway service.
  artifacts:
    - file: zuul/src/main/java/net/devh/ZuulApplication.java
      lines: [17]
    - file: zuul/src/main/resources/bootstrap.yml
      lines: [2]

2:
  status: disregarded
  argument: The services do not authenticate requests mutually. Internal requests are sent via plain HTTP.

3:
  status: disregarded
  argument: No authentication mechanism is deployed.

4:
  status: disregarded
  argument: External entities are not represented in the application. Users behave transparent to the server.

5:
  status: disregarded
  argument: No authentication tokens are used.

6:
  status: disregarded
  argument: No rate limiting of any means is deployed.

7:
  status: disregarded
  argument: User external entities can call the gateway service using an unencrypted HTTP connection.
  artifacts:
    - file: zuul/src/main/resources/bootstrap.yml
      lines: [2]

8:
  status: disregarded
  argument: All internal services communicate over insecure HTTP connections.
  artifacts:
    - file: service-a/src/main/resources/bootstrap.yml
      lines: [2]

9:
  status: disregarded
  argument: No central logging system is deployed.

10:
  status: disregarded
  argument: No central logging system is deployed.

11:
  status: disregarded
  argument: Logs are not explicitly sanitized.

12:
  status: disregarded
  argument: No explicit logging mechanism is deployed.

13:
  status: observed
  argument: The internal service with indirect connection to the user (via Zuul) is protected using the Hystrix circuit breaker. Note however, that the circuit breaker is not deployed at the proxy node, but at the internal service.
  artifacts:
    - file: service-a/src/main/java/net/devh/A1ServiceApplication.java
      lines: [30]

14:
  status: observed
  argument: The Zuul API gateway performs load balancing using Ribbon by default.
  artifacts:
    - file: zuul/src/main/java/net/devh/ZuulApplication.java
      lines: [17]

15:
  status: not applicable
  argument: Not a service mesh deployment.

16:
  status: observed
  argument:
    - Registry Service (Eureka Server) with @EnableEurekaServer present.
    - Started in Docker Container through Compose, thus deployable on dedicated server
  artifacts:
    - file: eureka-server/src/main/java/net/devh/EurekaServeApplication.java
      lines: [10]
    - file: docker-compose.yml
      lines: [15]

17:
  status: disregarded
  argument: No HTTP basic password listed in any YML-Configuration of format username:password@here-location-of-eureka-server at "eureka.client.serviceUrl.defaultZone".
  artifacts:
    - file: eureka-server/src/main/java/net/devh/EurekaServeApplication.java
      lines: [10]
    - file: eureka-server/src/main/resources/bootstrap.yml
      lines: [39, 49]

18:
  status: disregarded
  argument: No secret manager is deployed.
